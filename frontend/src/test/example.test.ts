import { describe, it, expect } from "vitest";

// If you still see errors, try the explicit type import for the functions:
// import { describe, it, expect } from "vitest";

describe("example", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });
});